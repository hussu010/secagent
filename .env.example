# Copy to .env and fill in. .env is gitignored — never commit real keys.
# Used by `secagent annotate` (page summaries) and `secagent hunt` (the v2 hunter).

ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the hunter model (default: claude-opus-4-8).
# SECAGENT_MODEL=claude-opus-4-8
